#include "main.h"
#include <stdio.h>

/**
 * add - A function that adds 2 integers and returns the result
 * @a: the int to be checked
 * @b: the other int to be checked
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
